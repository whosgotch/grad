GM.Name = "prop"
GM.Author = "whosgotch"

prop = prop or {}

prop.teams = {stores={}, categories={}}
prop.spawns = {points={}}
prop.net = {registry={}}
prop.data = {fields={}, cache={}}
prop.command = {stored={}}
prop.state = {registry={}}

print("[prop] Core Tables Initialized...")